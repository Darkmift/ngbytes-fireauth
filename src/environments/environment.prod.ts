// you need to create this file -> config/firebase.config.json
import * as firebaseConfig from '../config/firebase.config.json';

export const environment = {
  firebase: firebaseConfig,
  production: true,
};
